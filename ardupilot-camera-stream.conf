<!DOCTYPE busconfig PUBLIC
 "-//freedesktop//DTD D-BUS Bus Configuration 1.0//EN"
 "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd">
<busconfig>
	<policy context="default">
		<allow own="org.ardupilot.camera.stream"/>
		<allow send_destination="org.ardupilot.camera.stream"/>
		<allow receive_sender="org.ardupilot.camera.stream"/>
	</policy>
</busconfig>

